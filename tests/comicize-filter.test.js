const assert=require("assert"),fs=require("fs"),vm=require("vm");
const sandbox={window:{},Uint8ClampedArray,Float64Array,Math,Set,Object};vm.createContext(sandbox);vm.runInContext(fs.readFileSync("comicize-filter.js","utf8"),sandbox);const C=sandbox.window.ComicizeFilter,plain=x=>JSON.parse(JSON.stringify(x));
assert.deepStrictEqual(plain(C.normalizeComicizeParams({colorBrush:-1,dotDensity:150,dotFade:-2})),{enabled:true,colorBrush:0,dotDensity:100,dotFade:0,debugMode:"none",quantizeSmallRadii:false});
const grid640=C.getComicizeGrid(640,320),grid1280=C.getComicizeGrid(1280,640);assert.strictEqual(grid640.tileSize,10);assert.strictEqual(grid1280.tileSize,20,"grid spacing scales with current dimensions");assert.notStrictEqual(C.getComicizeGrid(640,333).originY,0,"short-axis lattice is centered independently");
const makeUniform=(width,height,value)=>{const source=new Uint8ClampedArray(width*height*4);for(let i=0;i<source.length;i+=4)source.set([value,value,value,90+(i/4)%100],i);return source;};
const source=makeUniform(128,64,160);for(const params of [{enabled:false},{dotFade:100}])assert.deepStrictEqual(Array.from(C.applyComicizeToBuffer(source,128,64,params)),Array.from(source));
const lowDensity=C.analyzeComicizeTiles(source,128,64,{dotDensity:0}),highDensity=C.analyzeComicizeTiles(source,128,64,{dotDensity:100});assert.deepStrictEqual(lowDensity.tiles.map(t=>[t.centerX,t.centerY]),highDensity.tiles.map(t=>[t.centerX,t.centerY]),"density does not alter grid positions");assert.ok(highDensity.tiles.some((tile,index)=>tile.radius>lowDensity.tiles[index].radius),"density changes radius response");
const center=lowDensity.tiles.reduce((a,b)=>Math.abs(b.centerX-64)+Math.abs(b.centerY-32)<Math.abs(a.centerX-64)+Math.abs(a.centerY-32)?b:a),corner=lowDensity.tiles[0];assert.ok(corner.radius>center.radius,"uniform image has stronger corner dots from vignette");
const dark=C.analyzeComicizeTiles(makeUniform(128,64,32),128,64,{dotDensity:50}),bright=C.analyzeComicizeTiles(makeUniform(128,64,224),128,64,{dotDensity:50});assert.ok(dark.tiles[Math.floor(dark.tiles.length/2)].radius>bright.tiles[Math.floor(bright.tiles.length/2)].radius,"dark tile average makes a larger dot");
for(const tile of highDensity.tiles.slice(0,20))assert.ok(tile.pieceRadii.every(radius=>radius===tile.radius),"center and four owned quarters share one radius");
const varied=new Uint8ClampedArray(128*64*4);for(let y=0;y<64;y++)for(let x=0;x<128;x++){const i=(y*128+x)*4,v=(x<64?35:220)+(y<32?0:25);varied.set([v,v,v,255],i);}const analyzed=C.analyzeComicizeTiles(varied,128,64,{dotDensity:30}),shared=analyzed.tiles.filter(t=>Math.abs(t.centerX-64)<=2&&Math.abs(t.centerY-32)<=2);assert.ok(new Set(shared.map(t=>t.radius.toFixed(6))).size>1,"four neighboring tiles at a shared corner may retain different quarter radii");
const result=C.applyComicizeToBuffer(varied,128,64,{dotDensity:50,dotFade:0});assert.strictEqual(result.length,varied.length);for(let i=3;i<result.length;i+=4)assert.strictEqual(result[i],varied[i],"alpha preserved");
const html=fs.readFileSync("index.html","utf8");assert.match(html,/Comicize=\$\{p\.enabled\?1:0\}/);assert.match(html,/applyComicizeToCanvas/);assert.match(html,/<b>Effects &amp; Finishes<\/b>[\s\S]*btnComicizeMode/);assert.match(html,/comicizeColorBrushInput[^>]*disabled/);
console.log("comicize-filter tests passed");
